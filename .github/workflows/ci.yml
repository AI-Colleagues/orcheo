name: CI

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
      - 'LICENSE'
      - '.cursor/**'
      - '.cursorrules'
      - '.cursorignore'
      - '.devcontainer/**'
      - '.readthedocs.yaml'
      - 'mkdocs.yml'
      - '.github/ISSUE_TEMPLATE/**'
    tags:
      - 'core-v*'
      - 'backend-v*'
      - 'sdk-v*'
      - 'agentensor-v*'
      - 'studio-v*'
      - 'stack-v*'
      - 'desktop-v*'
  pull_request:
    branches:
      - main
    paths-ignore:
      - '**/*.md'
      - 'docs/**'
      - 'LICENSE'
      - '.cursor/**'
      - '.cursorrules'
      - '.cursorignore'
      - '.devcontainer/**'
      - '.readthedocs.yaml'
      - 'mkdocs.yml'
      - '.github/ISSUE_TEMPLATE/**'
    types:
      - opened
      - synchronize
      - ready_for_review

permissions:
  id-token: write  # Required for PyPI trusted publishing
  contents: read

jobs:
  lint:
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --all-groups

      - name: Check style against standards
        run: uv run make lint

  app-gateway:
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install workspace dependencies
        run: uv sync --all-packages --all-groups

      - name: Lint and type-check gateway
        run: |
          uv run ruff check apps/app_gateway/src src/orcheo/hosted_apps tests/hosted_apps
          uv run mypy apps/app_gateway/src/orcheo_app_gateway src/orcheo/hosted_apps/gateway.py

      - name: Test gateway and Hosted Apps security contracts
        run: uv run pytest tests/hosted_apps -q

      - name: Build app gateway image
        run: docker build -f Dockerfile.app-gateway -t orcheo-app-gateway:${{ github.sha }} .

      - name: Verify app gateway image
        run: |
          docker run --rm \
            --env ORCHEO_HOSTED_APPS_ENABLED=false \
            orcheo-app-gateway:${{ github.sha }} \
            python -c "from orcheo_app_gateway.main import create_app; assert create_app().title"

      - name: Generate image SBOM
        uses: anchore/sbom-action@v0
        with:
          image: orcheo-app-gateway:${{ github.sha }}
          artifact-name: app-gateway-sbom.spdx.json
          format: spdx-json

      - name: Scan image for known vulnerabilities
        uses: aquasecurity/trivy-action@a9c7b0f06e461e9d4b4d1711f154ee024b8d7ab8 # v0.36.0
        with:
          image-ref: orcheo-app-gateway:${{ github.sha }}
          format: sarif
          output: app-gateway-trivy.sarif
          severity: HIGH,CRITICAL
          scanners: vuln
          ignore-unfixed: true
          limit-severities-for-sarif: true
          exit-code: "1"

      - name: Upload vulnerability results
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: app-gateway-trivy.sarif

  coverage-tests:
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - shard: backend
            paths: tests/backend
          - shard: nodes
            paths: tests/nodes
          - shard: sdk
            paths: tests/sdk
          - shard: core
            paths: >-
              tests/graph tests/listeners tests/triggers tests/triggers_layer
              tests/runtime tests/sandbox tests/workspace tests/agentensor
              tests/plugins tests/config tests/edges tests/identity
              tests/integration tests/widgets tests/security tests/test_*.py

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --all-groups

      - name: Run unit tests with coverage
        env:
          COVERAGE_FILE: .coverage.${{ matrix.shard }}
        run: uv run pytest ${{ matrix.paths }} --cov --cov-context=test --cov-report= -n auto

      - name: Store coverage data
        uses: actions/upload-artifact@v4
        with:
          name: coverage-data-${{ matrix.shard }}
          path: .coverage.${{ matrix.shard }}
          include-hidden-files: true

  coverage:
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    needs: coverage-tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --all-groups

      - name: Download coverage data
        uses: actions/download-artifact@v4
        with:
          pattern: coverage-data-*
          path: coverage-data
          merge-multiple: true

      - name: Build coverage reports
        run: |
          uv run coverage combine coverage-data
          uv run coverage html --show-contexts --title "Coverage for ${{ github.sha }}"
          uv run coverage xml
          uv run diff-cover coverage.xml --format html:index.html

      - name: Store coverage html
        uses: actions/upload-artifact@v4
        with:
          name: coverage-html
          path: htmlcov
          include-hidden-files: true

      - name: Store diff coverage html
        uses: actions/upload-artifact@v4
        with:
          name: diff-coverage-html
          path: index.html

      - name: Check coverage
        run: |
          uv run coverage report --fail-under 95
          uv run diff-cover coverage.xml --fail-under 100

  postgres-persistence:
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    timeout-minutes: 5

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --all-groups

      - name: Run Postgres persistence checks
        env:
          # Use a numeric address so psycopg never enters an unbounded DNS lookup.
          ORCHEO_POSTGRES_DSN: postgresql://postgres:postgres@127.0.0.1:5432/postgres
          ORCHEO_CHECKPOINT_BACKEND: postgres
          ORCHEO_TEST_POSTGRES_PERSISTENCE: "1"
        run: |
          uv run pytest \
            tests/integration/test_postgres_persistence.py \
            tests/hosted_apps/test_postgres_store.py \
            -vv \
            -o faulthandler_timeout=60

  build-and-release:
    needs: [lint, coverage, postgres-persistence]
    if: "startsWith(github.ref, 'refs/tags/core-v') || startsWith(github.ref, 'refs/tags/backend-v') || startsWith(github.ref, 'refs/tags/sdk-v') || startsWith(github.ref, 'refs/tags/agentensor-v')"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Determine package context
        id: package
        run: |
          tag="${GITHUB_REF_NAME}"
          if [[ "$tag" == core-v* ]]; then
            echo "package=orcheo" >> "$GITHUB_OUTPUT"
            echo "project_file=pyproject.toml" >> "$GITHUB_OUTPUT"
            echo "tag_prefix=core-v" >> "$GITHUB_OUTPUT"
          elif [[ "$tag" == backend-v* ]]; then
            echo "package=orcheo-backend" >> "$GITHUB_OUTPUT"
            echo "project_file=apps/backend/pyproject.toml" >> "$GITHUB_OUTPUT"
            echo "tag_prefix=backend-v" >> "$GITHUB_OUTPUT"
          elif [[ "$tag" == sdk-v* ]]; then
            echo "package=orcheo-sdk" >> "$GITHUB_OUTPUT"
            echo "project_file=packages/sdk/pyproject.toml" >> "$GITHUB_OUTPUT"
            echo "tag_prefix=sdk-v" >> "$GITHUB_OUTPUT"
          elif [[ "$tag" == agentensor-v* ]]; then
            echo "package=agentensor" >> "$GITHUB_OUTPUT"
            echo "project_file=packages/agentensor/pyproject.toml" >> "$GITHUB_OUTPUT"
            echo "tag_prefix=agentensor-v" >> "$GITHUB_OUTPUT"
          else
            echo "Unsupported tag prefix: $tag" >&2
            exit 1
          fi
          echo "dist_dir=dist/${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"

      - name: Install dependencies
        run: uv sync --all-groups

      - name: Verify package version matches tag
        env:
          PROJECT_FILE: ${{ steps.package.outputs.project_file }}
          TAG_PREFIX: ${{ steps.package.outputs.tag_prefix }}
          TAG_NAME: ${{ github.ref_name }}
        run: |
          uv run python - <<'PY'
          import os
          import re
          import tomllib
          from pathlib import Path
          from packaging.version import InvalidVersion, Version

          tag_name = os.environ["TAG_NAME"]
          tag_prefix = os.environ["TAG_PREFIX"]
          if not tag_name.startswith(tag_prefix):
              raise SystemExit(f"Tag {tag_name!r} does not start with {tag_prefix!r}.")
          tag_version = tag_name.removeprefix(tag_prefix)
          pattern = r"\d+\.\d+\.\d+(?:-(?:alpha|beta|rc)\.\d+)?"
          if re.fullmatch(pattern, tag_version) is None:
              raise SystemExit(
                  "Release tags must use X.Y.Z or X.Y.Z-{alpha,beta,rc}.N."
              )

          project = tomllib.loads(
              Path(os.environ["PROJECT_FILE"]).read_text(encoding="utf-8")
          )
          package_version = project["project"]["version"]
          try:
              matches = Version(tag_version) == Version(package_version)
          except InvalidVersion as exc:
              raise SystemExit(f"Invalid release version: {exc}") from exc
          if not matches:
              raise SystemExit(
                  f"Tag version {tag_version} does not match package version "
                  f"{package_version}."
              )
          PY

      - name: Build ${{ steps.package.outputs.package }}
        run: |
          uv build --package "${{ steps.package.outputs.package }}" --out-dir "${{ steps.package.outputs.dist_dir }}"

      - name: Publish ${{ steps.package.outputs.package }} to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: ${{ steps.package.outputs.dist_dir }}

  stack-image-metadata:
    needs: [lint, coverage]
    if: "startsWith(github.ref, 'refs/tags/stack-v')"
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.meta.outputs.version }}
      channel: ${{ steps.meta.outputs.channel }}
      phase: ${{ steps.meta.outputs.phase }}
      package_requirements: ${{ steps.packages.outputs.requirements }}
      studio_version: ${{ steps.studio-package.outputs.version }}
      stack_repo: ${{ steps.meta.outputs.stack_repo }}
      studio_repo: ${{ steps.meta.outputs.studio_repo }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v5

      - name: Determine image metadata
        id: meta
        run: |
          tag="${GITHUB_REF_NAME}"
          version="${tag#stack-v}"
          if [[ "$version" == "$tag" || -z "$version" ]]; then
            echo "Invalid stack tag: $tag" >&2
            exit 1
          fi
          if [[ "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            channel="stable"
            phase=""
          elif [[ "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+-(alpha|beta|rc)\.[0-9]+$ ]]; then
            channel="prerelease"
            phase="${BASH_REMATCH[1]}"
          else
            echo "Stack versions must use X.Y.Z or X.Y.Z-{alpha,beta,rc}.N: $version" >&2
            exit 1
          fi
          owner_lc=$(printf '%s' "${GITHUB_REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "channel=${channel}" >> "$GITHUB_OUTPUT"
          echo "phase=${phase}" >> "$GITHUB_OUTPUT"
          echo "stack_repo=ghcr.io/${owner_lc}/orcheo-stack" >> "$GITHUB_OUTPUT"
          echo "studio_repo=ghcr.io/${owner_lc}/orcheo-studio" >> "$GITHUB_OUTPUT"

      - name: Resolve first-party Python packages
        id: packages
        run: |
          uv run --no-project --with packaging \
            python deploy/stack/resolve_orcheo_packages.py \
            --channel "${{ steps.meta.outputs.channel }}" \
            --repository-root . \
            --output /tmp/orcheo-requirements.txt \
            --studio-output /tmp/orcheo-studio-version.txt
          requirements=$(tr '\n' ' ' < /tmp/orcheo-requirements.txt)
          echo "requirements=${requirements% }" >> "$GITHUB_OUTPUT"

      - name: Resolve Studio package
        id: studio-package
        run: |
          version=$(tr -d '\n' < /tmp/orcheo-studio-version.txt)
          echo "version=${version}" >> "$GITHUB_OUTPUT"

  image-build:
    needs: [stack-image-metadata]
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        image:
          - name: stack
            context: deploy/stack
            file: deploy/stack/Dockerfile.orcheo
            repo: ${{ needs.stack-image-metadata.outputs.stack_repo }}
          - name: studio
            context: deploy/stack
            file: deploy/stack/Dockerfile.studio
            repo: ${{ needs.stack-image-metadata.outputs.studio_repo }}
        platform:
          - runner: ubuntu-latest
            arch: amd64
          - runner: ubuntu-24.04-arm
            arch: arm64
    runs-on: ${{ matrix.platform.runner }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ${{ matrix.image.name }} by digest
        id: build
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.image.context }}
          file: ${{ matrix.image.file }}
          platforms: linux/${{ matrix.platform.arch }}
          build-args: |
            ORCHEO_RELEASE_CHANNEL=${{ needs.stack-image-metadata.outputs.channel }}
            ORCHEO_PACKAGE_REQUIREMENTS=${{ needs.stack-image-metadata.outputs.package_requirements }}
            ORCHEO_STUDIO_VERSION=${{ needs.stack-image-metadata.outputs.studio_version }}
          outputs: type=image,name=${{ matrix.image.repo }},push-by-digest=true,name-canonical=true,push=true

      - name: Export digest
        run: |
          mkdir -p /tmp/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "/tmp/digests/${digest#sha256:}"

      - name: Upload digest
        uses: actions/upload-artifact@v4
        with:
          name: digests-${{ matrix.image.name }}-${{ matrix.platform.arch }}-${{ github.run_attempt }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

  image-merge:
    needs: [stack-image-metadata, image-build]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        image:
          - name: stack
            repo: ${{ needs.stack-image-metadata.outputs.stack_repo }}
          - name: studio
            repo: ${{ needs.stack-image-metadata.outputs.studio_repo }}
    steps:
      - name: Download digests
        uses: actions/download-artifact@v4
        with:
          path: /tmp/digests
          pattern: digests-${{ matrix.image.name }}-*-${{ github.run_attempt }}
          merge-multiple: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create manifest list and push
        working-directory: /tmp/digests
        run: |
          repo="${{ matrix.image.repo }}"
          version="${{ needs.stack-image-metadata.outputs.version }}"
          channel="${{ needs.stack-image-metadata.outputs.channel }}"
          phase="${{ needs.stack-image-metadata.outputs.phase }}"
          tag_args=(-t "${repo}:${version}")
          if [[ "$channel" == "stable" ]]; then
            tag_args+=(-t "${repo}:latest")
          else
            tag_args+=(-t "${repo}:prerelease" -t "${repo}:${phase}")
          fi
          docker buildx imagetools create "${tag_args[@]}" \
            $(printf "${repo}@sha256:%s " *)

      - name: Inspect image
        run: |
          docker buildx imagetools inspect "${{ matrix.image.repo }}:${{ needs.stack-image-metadata.outputs.version }}"

  studio:
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/studio
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          token: ""
      - name: Upgrade npm
        run: npm install -g npm@^11.5.1
      - name: Install dependencies
        run: |
          npm ci
      - name: Lint
        run: npm run lint
      - name: Test
        run: npm run test -- --run

  studio-build-and-release:
    needs: [studio]
    if: "startsWith(github.ref, 'refs/tags/studio-v')"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/studio
    permissions:
      contents: read
      id-token: write  # Required for npm trusted publishing and provenance
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: 'https://registry.npmjs.org'
      - name: Upgrade npm
        run: npm install -g npm@^11.5.1
      - name: Install dependencies
        run: |
          npm ci
      - name: Verify version matches tag
        id: version
        run: |
          tag="${GITHUB_REF_NAME}"
          version="${tag#studio-v}"
          pkg_version=$(node -p "require('./package.json').version")
          if [[ "$version" != "$pkg_version" ]]; then
            echo "Error: Tag version ($version) does not match package.json version ($pkg_version)" >&2
            exit 1
          fi
          if [[ "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            npm_tag="latest"
          elif [[ "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+-(alpha|beta|rc)\.[0-9]+$ ]]; then
            npm_tag="prerelease"
          else
            echo "Studio versions must use X.Y.Z or X.Y.Z-{alpha,beta,rc}.N: $version" >&2
            exit 1
          fi
          echo "npm_tag=${npm_tag}" >> "$GITHUB_OUTPUT"
          echo "Version check passed: $version"
      - name: Build
        run: npm run build
      - name: Publish to npm
        run: npm publish --provenance --access public --tag "${{ steps.version.outputs.npm_tag }}"

  desktop-tauri-release:
    needs: [lint, coverage, postgres-persistence, studio]
    if: "startsWith(github.ref, 'refs/tags/desktop-v')"
    runs-on: macos-26
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: |
            apps/studio/package-lock.json
            apps/desktop/tauri/package-lock.json

      - name: Upgrade npm
        run: npm install -g npm@^11.5.1

      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          python-version: '3.12'

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop/tauri/src-tauri -> target

      - name: Verify Apple Silicon runner
        run: |
          test "$(uname -m)" = "arm64"

      - name: Install dependencies
        run: |
          npm ci --prefix apps/desktop/tauri

      - name: Verify desktop version matches tag
        run: |
          version="${GITHUB_REF_NAME#desktop-v}"
          pkg_version=$(node -p "require('./apps/desktop/tauri/package.json').version")
          tauri_version=$(node -p "require('./apps/desktop/tauri/src-tauri/tauri.conf.json').version")
          if [[ ! "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-((alpha|beta|rc)\.[0-9]+))?$ ]]; then
            echo "Desktop versions must use X.Y.Z or X.Y.Z-{alpha,beta,rc}.N: $version" >&2
            exit 1
          fi
          if [[ "$version" != "$pkg_version" ]]; then
            echo "Error: Tag version ($version) does not match package.json version ($pkg_version)" >&2
            exit 1
          fi
          if [[ "$version" != "$tauri_version" ]]; then
            echo "Error: Tag version ($version) does not match tauri.conf.json version ($tauri_version)" >&2
            exit 1
          fi
          echo "Version check passed: $version"

      - name: Build Apple Silicon DMG
        env:
          ORCHEO_TAURI_USE_PUBLISHED_RELEASES: "true"
        run: npm --prefix apps/desktop/tauri run build:dmg

      - name: Stage DMG release asset
        run: |
          mkdir -p dist/desktop-tauri
          find apps/desktop/tauri/src-tauri/target/release/bundle/dmg \
            -maxdepth 1 \
            -type f \
            -name '*.dmg' \
            -exec cp {} dist/desktop-tauri/ \;
          dmg_count=$(find dist/desktop-tauri -maxdepth 1 -type f -name '*.dmg' | wc -l | tr -d ' ')
          if [[ "$dmg_count" -eq 0 ]]; then
            echo "No DMG files were produced." >&2
            exit 1
          fi
          if find dist/desktop-tauri -maxdepth 1 -type f ! -name '*.dmg' | grep -q .; then
            echo "Only DMG files may be staged as desktop release assets." >&2
            find dist/desktop-tauri -maxdepth 1 -type f >&2
            exit 1
          fi
          ls -lh dist/desktop-tauri

      - name: Upload DMG to GitHub release
        uses: softprops/action-gh-release@v2
        with:
          draft: false
          prerelease: ${{ contains(github.ref_name, '-alpha.') || contains(github.ref_name, '-beta.') || contains(github.ref_name, '-rc.') }}
          fail_on_unmatched_files: true
          files: dist/desktop-tauri/*.dmg
          name: Orcheo Desktop ${{ github.ref_name }}
          body: See the DMG asset below to download Orcheo Desktop for Apple Silicon.
