[bumpversion]
current_version = 0.29.0-alpha.4
commit = true
tag = true
tag_name = stack-v{new_version}
message = Bump stack version to {new_version}
parse = (?P<major>\d+)\.(?P<minor>\d+)\.(?P<patch>\d+)(?:-(?P<prerel>alpha|beta|rc)\.(?P<prerel_num>\d+))?
serialize = {major}.{minor}.{patch}-{prerel}.{prerel_num}
	{major}.{minor}.{patch}

[bumpversion:part:prerel]
values = alpha
	beta
	rc
	final
optional_value = final

[bumpversion:part:prerel_num]
first_value = 1
